/**
 * This stub is provided to make it straightforward to get started.
 */

export function twoFer(receiver? : string) : string {
  return 'One for ' + (receiver ? receiver : "you") + ', one for me.'
}
